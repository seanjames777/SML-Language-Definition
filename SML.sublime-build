{
    "cmd": ["sml", "$file"],
    "selector": "source.sml",
    "working_dir": "$file_path",

    "osx": {
        "cmd": ["$packages/SML/sml_mac.sh", "$file"]
    },

    "windows": {
        "cmd": ["sml.bat", "$file"]
    }
}
