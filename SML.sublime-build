{
    "cmd": ["sml", "$file"],
    "selector": "source.sml",
    "working_dir": "$file_path",

    "osx": {
        "cmd": ["sh", "$packages/SML (Standard ML)/sml_mac.sh", "$file"]
    },

    "windows": {
        "cmd": ["sml.bat", "$file"]
    },

    "file_regex": "(.+):([0-9]+)\\.([0-9]+)(?:\\-[0-9]+\\.[0-9]+)? (.+)"
}
